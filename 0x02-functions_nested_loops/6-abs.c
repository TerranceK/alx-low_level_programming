#include"main.h"

/**
 * _abs - Returns absolute value of a given integer.
 *
 * @n: Input value to be "absoluted".
 * Return: absolute value (int).
 */

int _abs(int n)
{

	if (n > 0)
	{
		n = n;
	}
	else
	{
		n *= -1;
	}

	return (n);
}

